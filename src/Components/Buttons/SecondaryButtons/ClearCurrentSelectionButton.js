import { Button } from '@material-ui/core';

const ClearCurrentSelectionButton = props => {
    return (
        <Button color="secondary" variant="contained">
            Clear Current Selection
        </Button>
    )
}

export default ClearCurrentSelectionButton;