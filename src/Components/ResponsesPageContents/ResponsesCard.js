import { Card } from "@material-ui/core";
import React, { useContext } from "react";

const ResponseCard = () => {
  // const { questionNumber, questionTopic, userResponses } = props;
  return (
    <Card>
      <h1>ResponseCard</h1>
      {/* <h3>{questionNumber}</h3>
      <h3>{questionTopic}</h3>
      <h3>{JSON.stringify(userResponses)}</h3> */}
    </Card>
  );
};

export default ResponseCard;
